node_modules
dist
.output
.vinxi
pnpm-lock.yaml
package-lock.json
routeTree.gen.ts

# Email HTML is hand-tuned with inline styles for client compatibility —
# prettier would reformat in ways that may break rendering in older email
# clients (Outlook, etc.). Keep these files untouched by the formatter.
supabase/email-templates/

# Auto-generated by Supabase (regenerated on every schema change, including
# by Lovable). Formatting it just causes CI churn.
src/integrations/supabase/types.ts

# Managed by Lovable — touched every session, not worth running prettier on.
.lovable/
